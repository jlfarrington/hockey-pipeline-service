describe('playerPipelineController: getPlayerDetails', () => {
  it('should return a 200 from API when provided a player ID and season year', () => {});

  it('should write a .csv to project folder', () => {});

  it('should handle errors', () => {});

  it('should pass a 422 if player ID and season year are not provided', () => {});
});
